# 💡 FSky7.ErrorPage

FSky7 项目所使用的 HTTP 的错误页面。

## 🧱 Build 

运行 `php build.php` 进行一键构建。

## 📦 Releases

前往 Releases 页面获取打包好的错误页面。

## 📝 Custom

修改 `theme.html` 和 `errors.json` 以自定义。
